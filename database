create table user
(user_id int not null,
pref varchar(2),
location varchar(30),
gender varchar(2),
age int
);

create table hobbies(
rel_id int not null,
user int not null,
foreign key (user) references user (user_id)
)
